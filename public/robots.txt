User-agent: *
Allow: /

# Point crawlers to the sitemap (relative path). If you have a canonical site URL, you
# can replace this with the absolute URL: Sitemap: https://example.com/api/sitemap.xml
Sitemap: https://www.franceparts.be/api/sitemap.xml

# Optional crawl-delay for slower crawlers (unofficial, used by some engines). Uncomment to enable.
# Crawl-delay: 10
